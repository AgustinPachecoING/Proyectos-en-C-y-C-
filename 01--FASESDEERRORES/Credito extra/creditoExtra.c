#include <stdio.h>
#include "studio.h"

int main() {
    int resultado = sumarEnteros(10, 5);
    printf("Resultado de la suma: %d\n", resultado);

    concatenarCaracteres();

    return 0;
}